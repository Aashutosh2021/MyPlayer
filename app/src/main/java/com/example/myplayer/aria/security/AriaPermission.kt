package com.example.myplayer.aria.security

enum class AriaPermission {
    /** Standard playback, search, and metadata operations. */
    STANDARD,
    
    /** Destructive or administrative operations (e.g. deleting downloaded files). */
    ADMIN
}
